# Entropy Layer

Module: `flow_props.entropy_layer`

## Purpose

This module computes entropy-layer thickness from wall-normal profiles.

## Main Functions

- `entropy_difference`
- `entropy_layer_thickness`
- `run_entropy`

## Current Criterion

The current public constant is:

- `ENTROPY_LAYER_WALL_FRACTION`

This corresponds to the normalized wall-entropy criterion described in the theory section.